@page
@using IT_Center_Website.Components.LearnCourse;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers;
@model IT_Center_Website.Pages.Learner.LearnCourse.IndexModel;
@{
    
}

@foreach (var lesson in Model.Lessons)
{
        <component type="typeof(VideoSection)" render-mode="ServerPrerendered"
        param-VideoMaterial="@(lesson.MaterialUrl)" />
}

